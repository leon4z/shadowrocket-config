name: build

# 每天 UTC 23:00（北京 07:00，与上游 johnshall 的发布时刻一致）校验一次，
# 源文件改动时也立刻跑。校验失败不会发布，release 分支保持上一版可用。
on:
  schedule:
    - cron: '0 23 * * *'
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'scripts/**'
      - '.github/workflows/build.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 校验远程规则集并生成配置
        run: python3 scripts/build.py --report dist/report.json

      - name: 写入运行摘要
        if: always()
        run: python3 scripts/summary.py dist/report.json >> "$GITHUB_STEP_SUMMARY"

      - name: 发布到 release 分支
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          OUT="$RUNNER_TEMP/publish"
          mkdir -p "$OUT"
          cd "$OUT"
          git init -q
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git config user.name  'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'

          if git fetch -q --depth 1 origin release:refs/remotes/origin/release 2>/dev/null; then
            git checkout -q -b release refs/remotes/origin/release
          else
            git checkout -q --orphan release
            git rm -rq --cached . 2>/dev/null || true
          fi

          # 先清掉上一版的产物，避免改过文件名后旧配置留在 release 分支上
          rm -f ./*.conf
          cp "$GITHUB_WORKSPACE"/dist/*.conf ./
          git add -A

          if git diff --cached --quiet; then
            echo "配置无变化，跳过发布（上游与规格都没变）"
            exit 0
          fi

          git commit -q -m "build: $(date -u +%Y-%m-%dT%H:%M:%SZ) 上游 $(date -u +%Y-%m-%d) 校验 ${GITHUB_SHA::7}"
          git push -q origin release
          echo "已发布到 release 分支"
          ls -1 ./*.conf

      - name: 清理 jsDelivr 缓存
        # jsDelivr 对分支引用有缓存，不清的话镜像最长可能滞后十几小时。
        # 清失败不影响发布（raw 地址本来就不经过它）。
        run: |
          for f in "$GITHUB_WORKSPACE"/dist/*.conf; do
            n=$(basename "$f")
            if curl -sf --max-time 30 "https://purge.jsdelivr.net/gh/${GITHUB_REPOSITORY}@release/${n}" -o /dev/null; then
              echo "已清理 jsDelivr 缓存: $n"
            else
              echo "清理 jsDelivr 缓存失败（不影响发布）: $n"
            fi
          done
